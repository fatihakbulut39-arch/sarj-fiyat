name: 'Update Charging Prices'

on:
  schedule:
    # Her 3 günde bir UTC 02:00'de çalış (Türk saati: 05:00)
    - cron: '0 2 */3 * *'
  
  # Manual trigger
  workflow_dispatch:

jobs:
  update-prices:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Repo'yu checkout et
        uses: actions/checkout@v3
      
      - name: 🐍 Python kurulum
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: 📦 Bağımlılıkları yükle
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: 🔄 Fiyatları güncelle
        env:
          WORKER_URL: ${{ secrets.WORKER_URL }}
          CF_API_KEY: ${{ secrets.CF_API_KEY }}
        run: python update_cloudflare.py
      
      - name: 📝 Log kayıt et (başarılı)
        if: success()
        run: |
          echo "✅ Güncelleme başarılı - $(date)" >> update_log.txt
      
      - name: 📝 Log kayıt et (başarısız)
        if: failure()
        run: |
          echo "❌ Güncelleme başarısız - $(date)" >> update_log.txt
      
      - name: 📤 Log'u commit et
        if: always()
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add update_log.txt
          git diff --quiet && git diff --staged --quiet || git commit -m "📊 Fiyat güncellemesi - $(date +'%Y-%m-%d %H:%M:%S')"
          git push
        continue-on-error: true
